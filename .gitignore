# Secrets — never commit real credentials
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtual environments
venv/
venv_test/
.venv/
env/

# Vector store + logs (regenerated by `python ingest.py`, machine-specific)
chroma_store/
logs/

# Streamlit
.streamlit/secrets.toml

# OS / editor
.DS_Store
.vscode/
.idea/

# Note: PDF source documents in data/policies/ are intentionally NOT
# excluded here — they're committed to the repo for reproducibility.