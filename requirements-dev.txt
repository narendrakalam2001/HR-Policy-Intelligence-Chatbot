# Dev/test/CI-only dependencies. Not installed in the production Docker
# image — keeps the deployed container lean.
pytest==8.3.3
langchain-community==0.3.5  # provides FakeEmbeddings, used only by the test suite
ruff==0.7.4
